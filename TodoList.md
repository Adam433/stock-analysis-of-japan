## BMAD执行清单

### 当前判断依据

- 已有完整 BMAD 产物：`_bmad-output/planning-artifacts/prd.md`、`architecture.md`、`epics.md`
- 已有实现跟踪：`_bmad-output/implementation-artifacts/sprint-status.yaml`
- 当前请求不是从零规划新产品，而是对现有 brownfield 项目做一批功能增强和缺陷修复
- 按 `bmad-help` 的路由，最合适的起点是先用 `CC` 判断这批变更是否需要回写 PRD、UX、Architecture、Epics，然后再进入 `SP -> CS`

### 原始待办

1. 在K线图里面，只能显示一小段时间的K线，感觉数据不够，希望能够更多数据。具体放多少数据或者是否根据页面操作向后端发送请求获得更多数据按照常规方式做，这种应该是比较成熟的技术。
2. RPS图里面，RPS50、120、250 的标签在固定位置，而且在最近日期的曲线旁边，遮挡住了重要位置，应该进行调整。
3. K线图里面显示的日期格式不太好，弄成 `2026年03月04日` 这种格式就可以了，时间可以不要。
4. 顶上的导航栏，在数据健康页面是 `stockAnalyse 数据健康/策略配置/观察列表/回测` 都写到了 `日股数据的运营可信视图。` 里面，跟别的页面不一致，需要调整。
5. Universe 清单来源：`/Users/adam/Documents/GitHub/stockAnalyse/data/tse_common_stock_symbols.txt` 显示异常。另外不需要显示这个内容，只显示这个文件什么时候更新的。
6. 刷新执行状态里面看到并没有更新。最好是后端启动的时候自动更新，如果后端一直是启动状态，那就是每日自动更新。
7. 现在的 RPS 筛选规则里面没有指定是 RPS50、120、250。需要追加功能：
   - 可以手动输入是多少天的 RPS
   - 可以手动选择需要同时满足多少个 RPS
8. 观察列表页面，比如添加了 `1776.T`，但是不能从这个页面直接访问 `1776.T` 的具体信息，需要增加一个链接。
9. 数据健康页面 `已存日频行情涵盖 3832 只标的 普通股清单：71 只 ｜ 更新于 2026年4月14日 09:57` 这个信息很奇怪，感觉普通股清单数量不正常，需要修复一下。

### 分类

#### 功能增加

- `1` K线图增加更长历史或按交互加载更多数据
- `6` 刷新执行状态改为后端启动时自动更新，且在服务持续运行时每日自动更新
- `7` RPS 筛选支持自定义窗口天数，以及配置至少满足多少个 RPS 条件
- `8` 观察列表页增加股票详情入口

#### Bug修复 / 现有体验纠正

- `2` RPS 标签遮挡曲线
- `3` K线图日期格式不符合预期
- `4` 数据健康页顶部导航文案结构异常
- `5` Universe 清单来源显示异常，且展示内容应改为文件更新时间
- `9` 数据健康页普通股清单数量显示异常

### 用 bmad-help 判断后的命令路由

#### 总入口

1. `[CC]` `bmad-correct-course`
   - 用途：这是对既有 PRD / Architecture / Epics 的增量变更，不适合直接跳到 `CS`
   - 目标：先判断这 9 项里哪些只是 story 级修复，哪些已经构成需求、信息架构、技术方案或数据契约变化

#### 对“功能增加”的建议命令

- 对 `1 / 6 / 7`：
  - 首选链路：`CC -> EP -> CU -> CA -> CE -> SP -> CS`
  - 原因：
    - `1` 涉及图表数据加载策略和可能的 API 契约变化
    - `6` 涉及后端调度/启动行为变化
    - `7` 涉及核心筛选配置能力变化，属于产品能力扩展
- 对 `8`：
  - 默认链路：`CC -> CE -> SP -> CS`
  - 如果在 `CC` 中被认定只是现有页面可达性补全，可不单独跑 `EP/CA`

#### 对“Bug修复 / 现有体验纠正”的建议命令

- 对 `2 / 3 / 4`：
  - 默认链路：`CC -> CE -> SP -> CS`
  - 原因：更像现有 UI/文案/展示修正，通常不需要改 PRD 或 Architecture
- 对 `5 / 9`：
  - 建议链路：`CC -> CA -> CE -> SP -> CS`
  - 原因：这两项大概率不是纯前端文案问题，而是数据健康口径、统计逻辑、文件元数据读取方式存在偏差，可能需要技术方案校正

### 最优执行顺序

#### 第1步：变更分流

- 执行 `[CC]` `bmad-correct-course`
- 输入内容：
  - 本文件中的 9 项待办
  - 当前已有 BMAD 产物路径：
    - `_bmad-output/planning-artifacts/prd.md`
    - `_bmad-output/planning-artifacts/architecture.md`
    - `_bmad-output/planning-artifacts/epics.md`
    - `_bmad-output/implementation-artifacts/sprint-status.yaml`
- 预期输出：
  - 哪些项只要补 stories
  - 哪些项必须先补 PRD / UX / Architecture

#### 第2步：需求层补充

- 仅当 `CC` 判断功能边界已变化时执行 `[EP]` `bmad-edit-prd`
- 建议覆盖：
  - `1` 图表历史数据加载方式
  - `6` 刷新自动执行策略
  - `7` RPS 参数化筛选能力
  - `8` 观察列表到详情页的直接导航

#### 第3步：UX层补充

- 当 `CC` 认为这批变更包含明显交互/信息布局调整时执行 `[CU]` `bmad-create-ux-design`
- 建议覆盖：
  - `1` 图表默认显示范围与交互加载方式
  - `2` RPS 标签避让策略
  - `3` 日期格式
  - `4` 顶部导航一致性
  - `8` 观察列表详情跳转入口

#### 第4步：技术方案补充

- 当 `CC` 认定涉及数据契约、后台任务或统计逻辑变化时执行 `[CA]` `bmad-create-architecture`
- 建议覆盖：
  - `1` K线/RPS 历史数据查询边界与增量加载策略
  - `5` Universe 文件更新时间读取方式
  - `6` 后端启动刷新与每日自动刷新机制
  - `7` 参数化 RPS 规则的数据模型、API 请求结构、持久化影响
  - `9` 普通股清单统计逻辑与健康页展示口径

#### 第5步：拆分为可执行 stories

- 执行 `[CE]` `bmad-create-epics-and-stories`
- 建议按下面顺序拆：
  - 批次A：数据正确性修复
    - `5`
    - `9`
    - `6`
  - 批次B：图表与页面体验修复
    - `2`
    - `3`
    - `4`
    - `8`
  - 批次C：能力增强
    - `1`
    - `7`
- 这样排序的原因：
  - 先修正数据健康口径和刷新状态，避免后续页面验收建立在错误数据上
  - 再处理已有界面的可见缺陷
  - 最后做范围更大的能力增强，减少返工

#### 第6步：生成 sprint 顺序

- 执行 `[SP]` `bmad-sprint-planning`
- 目标：把 `CE` 产出的 stories 排成单一执行序列，并写回 sprint status

#### 第7步：创建首个故事

- 执行 `[CS]` `bmad-create-story`
- 默认选择 `SP` 产出的第一个 next story
- 本轮流程在 `CS` 执行完成后结束，不继续进入 `DS`

### 建议的首个 story 候选

- 第一优先：`9` 数据健康页普通股清单数量异常修复
  - 原因：这是明显的数据正确性问题，会影响对系统状态的判断
- 第二优先：`5` Universe 清单来源展示改为文件更新时间
  - 原因：与 `9` 同属数据健康页，可合并上下文
- 第三优先：`6` 刷新执行状态自动更新
  - 原因：补齐运营可信视图的核心时效性

### 需要你确认的位置

- 按当前 BMAD 路由，下一步应该先执行 `[CC]` `bmad-correct-course`
- 如果你确认要继续，我会以这 9 项待办和现有 `_bmad-output` 产物为输入，进入 `CC` 阶段
- 如果 `CC` 输出要求更新 PRD / UX / Architecture，我会按上面的顺序继续推进到 `CS` 结束
